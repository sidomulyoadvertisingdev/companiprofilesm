---
import MainLayout from "../../layouts/MainLayout.astro";
import JobDetail from "../../react-pages/JobDetail.jsx";

export function getStaticPaths() {
  return [{ params: { id: "1" } }];
}

const { id } = Astro.params;
---

<MainLayout>
  <JobDetail id={id} client:load />
</MainLayout>
